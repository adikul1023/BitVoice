{
  "name": "@securevoice/rendezvous-service",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch --experimental-strip-types src/server.ts",
    "build": "tsc --build",
    "typecheck": "tsc --build"
  }
}
